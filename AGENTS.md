# Project workflow

After completing a project update, run the relevant checks, commit the intended changes, and push to the configured GitHub remote. The user explicitly requested this workflow. Keep local credentials and `.local/` out of Git. Report the commit and push result; do not claim success when either fails.
